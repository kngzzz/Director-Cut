import logging

# --- No Tools Defined ---
# This agent uses callbacks for its logic and does not require external FunctionTools.

# Export the tools list for use in agent.py (must be defined, even if empty)
director_tools = []

logging.debug("Conversational director tools list is empty.")
